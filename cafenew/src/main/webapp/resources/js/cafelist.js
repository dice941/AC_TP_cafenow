/*var jsonArray = [];

var data = {name:'Samantha', age:60, gender:'girl'};
for (key in data) {
	console.log(data[key]);
}

var html = "";
for (var i=0; i<jsonArray.length; i++) {
	var data = ""
	for (key in jsonArray[i]) {
		data += key + " = " + jsonArray[i][key] + " ";
  }
  html += "<li>" + data + "</li>";
}
$('#list').append(html);

JSONParser jsonParser = new JSONParser();
JSONArray docuArray = (JSONArray) jsonObject.get("documents");
//documents만 뽑아오고  

JSONObject docuObject = (JSONObject) docuArray.get(i); 
//배열 i번째 요소 불러오고
         
logger.info(docuObject.get("keyValue").toString());
//뽑아오기 원하는 Key 이름을 넣어주면 그 value가 반환된다.*/